# test/known_failures.txt - THE BASELINE, COMMITTED.
#
# Until 19 September 2026 the gate had no baseline file. It compared each run to
# the PREVIOUS run recorded in memory/suite_runs.jsonl, which means the baseline
# was written by the run it judged. A failure was "new" exactly once; from the
# next run on it was known, forever, with nobody asked.
#
# Measured over the 58 recorded runs: 57 are VALID and TWENTY of them admitted
# node ids the previous run did not - +19 on 09-03, +23 on 09-06, +30 on 09-08,
# +18 on 09-14, +5 on 09-18. That is not a baseline, it is a ratchet.
#
# FORMAT: one pytest node id per line. Everything after '#' is the triage bucket
# and the reason. Blank lines and full-line comments are ignored.
#
# A LINE HERE IS A DEBT, NOT A DISPENSATION. Adding one is a human decision made
# in a commit; no run may write to this file. If a listed test goes green the
# gate says so and asks for the line to be removed, and if an id here no longer
# exists the gate says that too - a baseline that silently keeps dead entries is
# how 95 failures became invisible.
#
# Seeded from claude/reports/TEST_TRIAGE_2026-09-19.md, which has the full
# evidence for every bucket below.


# --- THE REMAINING ENTRIES ---
# The heading carried a count (13) that was already false by the time Half B ended
# and would have gone on being false at every removal. The bucket is on each line
# and the COUNT IS ASSERTED, in test_suite_gate_baseline.py, which is the only
# place a number is allowed to live: there it goes red when it stops being true.
test/test_cycle_reaper.py::test_end_to_end_a_spawned_cycle_leaves_its_exit_code_on_disk  # CLAIM_FALSE: verdict recorded 19 Sep, verified 19 Sep by running the chain. The triage said a cycle dies with no exit code on disk and the cause of death is unrecorded. It is recorded: the reaper writes TWICE - a provisional {state: WATCHING} BEFORE the wait (cycle_reaper.py:275, deliberate, so a reaper that dies mid-wait is visible), then the final {state: RECORDED, exit_code: N}. Observed live: WATCHING -> RECORDED exit_code=5. The TEST breaks out of its poll on record.exists(), so it reads the provisional record and KeyErrors on exit_code. Not fixed per the rule: the claim does not hold, so the cause is the test's poll condition and not a defect in the system. Left red rather than quietly rewritten - changing the test is a separate decision from the one this sweep was given.
test/test_direction_last.py::test_the_sealed_record_shows_direction_as_the_last_generated_field  # CLAIM_FALSE: verdict recorded 19 Sep, verified 19 Sep. The triage said "a sealed record with no direction field - it is absent". It is not absent: sealed_direction is PRESENT and None, which is the designed output of a refusal (market_bet.py:1337/1351/1445). Probed directly: outcome=REFUSED_NO_GROUNDED_CANDIDATE, n_snippets=1, n_segments=2 - the staged completion was not ADMITTED by the grounding gate, so win is None and every sealed_* field is None with it. Not fixed per the rule. The open question is why that completion fails the gate, which is a question about the grounding gate and not about whether direction is generated last - a different investigation.
test/test_script_suite.py::test_script_style_suite[test/test_needs_approvals.py]  # CLAIM_FALSE: verdict recorded 19 Sep, verified 19 Sep by rendering the brief. The triage said "an inline hint moved out of the brief instead of kept beside it". It is still there, reformatted: _push_text() renders "  приеми: OK 5dad    откажи: NO 5dad" (needs_report.py:867). The TEST asserts the old literal "(reply: OK 5dad)", which is prose, and the only other place that string survives is a COMMENT at needs_report.py:921 still describing the dead format - a false behaviour claim of exactly the kind the CLAUDE.md rule now forbids. Not fixed per the rule: the behaviour holds, so changing the assertion is a separate decision.

