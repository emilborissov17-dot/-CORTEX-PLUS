# HANDOVER — 2026-08-29

Written before the context ran out, on Emil's instruction: "Losing today's record
costs more than one more commit tonight."

Read this with `docs/QUEUE.md`. The queue is the map; this is what a fresh
session needs that the queue does not say in one place.

---

## THE TWO RULES DERIVED TODAY — apply these before anything else

**(a) A truncated grep cannot support the word "only".**
If a claim contains "the only", "nothing", or "zero of", the search behind it
runs untruncated or the claim is not made. This was written after I reported
"nothing in this repository can write the real ledger" — false. I had piped an
exhaustive search through `head -12`, and `scripts/openclaw_axis_worker.py:291`
was on line 13.

**(b) A conclusion from one line, where the answer needs two, is the same defect
in another shape.**
Written after I proposed fixing `orphan_scan`'s alias blindness by adding
`a.name` at `:114`. Insufficient — the match at `:187` is
`fn in direct and fn in cnames`, and `cnames` holds the CALL-SITE name. The
sibling branch at `:119-120` already stored both names, so the file contradicted
itself and one line could not show it. The third seat corrected this — Emil
relays between the three of us, he does not author the review; I had not opened
`:187`.

Both failures were mine, both were caught by someone else, and both were the same
root habit: reporting a partial read as a complete one.

---

## WHAT LANDED TODAY (branch `feature/lidaction-guard`, all pushed)

| commit | item |
|---|---|
| `6118e4b` | ITEM 3 closed — nine steps already applied, header was stale |
| `74af010` | ITEM 7.1 — K1 written every cycle, names its evidence |
| `584cbe4` | queue correction — branch was not still blocked |
| `e2c11b4` | ITEM 7.2 — K3 field on every claim, honestly 0 |
| `1e9b03c` | ITEM 7.3 — `prediction_resolutions` record created |
| `60de254` | ITEM 10 — `tools/suite_gate.py`, runs know if a cycle touched them |
| `3677c41` | K1 = 0.7425 from the first cycle-written run |
| `4a6ae17` | ITEM 11 — `resolve_ideas` wired; deadline date was wrong |
| `96597b7` | ITEM 23 — resolver stops failing soft; verdicts carry their series |
| `f4efaef` | hit rate restricted to leave-one-out survivors — **0.0%** |
| `d6bf2f5` | ITEM 21 — `feedback_loop` crash; ITEM 28 pre-registered |
| `62d4eb3` | ITEM 29 — 133 phase reports annotated, none rewritten |
| `3ec1b26` | ITEM 12(c) — `axis_history` stops deleting itself |
| `11bf1a4` | ITEM 33 — API transmits `measured:false` |
| `a0ffcea` | ITEM 34 premise did not survive its own stop-condition |
| `ad121e3` | ITEM 34-A — null score no longer averages unrelated metrics |
| `9b85408` | **ITEM 34 step 2 + ITEM 32 baseline** — scanner wired; `config/orphan_baseline.json` created |

**ITEM 38 Part 1 is built and its suite was running when this was written.**
`test/test_checkpoint_wiring.py`: `UNCOVERED_STEP_LIMIT = 31`, two assertions
(`<=` for rot, `==` for slack), both naming every uncovered step. Verified to
fail correctly by temporarily setting the limit back to 33. **It landed in the
commit that added this file** — the suite it was waiting on came back VALID at
12:45Z, 52 failed / 3454 passed, the only move against the 53 of `9b85408` being
`test_brain_scan`'s coin flip landing green. Uncovered count 31 against a limit
of 31: zero slack, and the assertion passes because the numbers meet, not
because there is room.

---

## CRITICAL: EVERY ORPHAN FIGURE BEFORE `9b85408` IS WRONG

`tools/orphan_scan.py` could not see wiring through an **aliased import** until
the fix in `9b85408`. Repairing it moved **77 entrypoints**:
`CALLED_IN_PRODUCTION` 202 → 286, `NEVER_CALLED` 55 → 54.

So every orphan number reported earlier today was computed with that blind spot
active, including:
- "18 dead `cortex_scoring_engine` scorers"
- "13 more in `cortex_planning_agent`"
- "11 uninstantiated `data_providers` classes"

**Re-deriving those against the fixed scanner is ITEM 25's first task.** Do not
act on the old figures.

---

## HELD, NOT DONE

**ITEM 14 — wire `tools/compass.py`.** Ruled and specified, not implemented.
Kimi: `HARDCODE-WITH-EXPIRY` and the detail-preserving `NOT_WIRED` shape.
Required:
- `k2()` returns `value=None, status=NOT_WIRED`, but **keeps** `promotions`,
  `withdrawals`, `last_promotion_ts`, plus a `consumers` field naming what was
  searched. Same shape as K4's refusal. Compass then reports **1 of 4**, not 2.
- `K2_NOT_WIRED_UNTIL = "2026-10-01"` plus a sibling `K2_NOT_WIRED_REASON`. A
  test fails on/after that date **and** fails if the date changes while the
  reason string does not — so bumping the date costs a sentence explaining why
  the world-check still cannot be written.
- Kimi's objection goes verbatim in the docstring: *"A constant with an expiry is
  still a constant, not a measurement... expiry day likely produces a date bump
  or removal rather than a real check - the placeholder becomes a recurring
  to-do that never graduates to computation."*
- Wire into all FIVE declaration sites, non-fatal on raise.
- **Remove the seven `tools/compass.py` entries from
  `config/orphan_baseline.json` in the same commit.** They carry
  `expires: "on ITEM 14 wiring"`. Leaving them is the slack defect again.
- Then run one real cycle and paste `memory/compass_latest.json`.

**ITEM 37 — RE-QUALIFY.** Kimi ruled the 20 TRUSTED sources were promoted under a
bug and a demotion event should be appended (not a rewrite). **Do not write it
yet** — it sits behind the finding below.

**My finding that blocks it:** the TRUSTED label has **no operational consumers**.
Untruncated census: 29 matches, of which 12 are comments, 7 are `compass`
reporting, 5 are `self_mirror` reporting, 3 are `openclaw_axis_worker`, and 4 are
`alignment/civilization_guard.py`'s own unrelated `TRUSTED_SOURCES` set. Every
downstream reader points at `axis_feeds.jsonl`; **none reads
`external_feeds.jsonl`**. So demoting all 20 breaks nothing — Kimi's
self-denial-of-service objection does not fire — but it also means K2 has never
gated anything, which is why K2 is being marked `NOT_WIRED`.

> **CORRECTED 2026-08-29, LATER THE SAME DAY, WHILE WIRING ITEM 14. The
> paragraph above is wrong in its central clause and is left standing because
> the shape of the error is the useful part.**
>
> `external_feeds.jsonl` **is** read. `scripts/openclaw_axis_worker.py:255-286`
> (`_peer_for`) reads it back for the incumbent value that every contradiction
> check is judged against, and `:313` sets
> `row["measured"] = (state == TRUSTED)`, which is what decides whether a
> reading lands in `external_feeds.jsonl` or is diverted to
> `external_shadow.jsonl`. **The label does gate behaviour.**
>
> I made rule (a)'s mistake in a new place: I counted MATCHES and reported a
> conclusion about READERS. Grepping `TRUSTED` finds where the word appears, not
> where the file it governs is opened — and `openclaw_axis_worker` appeared in
> that census as "3 matches", which I read as noise instead of opening it.
>
> **The true finding is narrower and stronger.** The label gates a code path,
> and *nothing in production calls that path*: an untruncated search for
> `openclaw_axis_worker` returns its own docstring, two comments in `core/`, one
> cockpit string, `docs/MODULE_MAP`, and three test files. No cycle, phase or
> scheduled task invokes `run()`. So K2 still gets `NOT_WIRED`, for a better
> reason: promotions accumulate into a path the system never walks.
>
> **AND IT CHANGES ITEM 37.** "Demoting all 20 breaks nothing" no longer
> follows. With 0 TRUSTED, every reading becomes SHADOW, `external_feeds.jsonl`
> stops growing, and `_peer_for` returns `None` — so no candidate can ever be
> contradicted, and none can ever be promoted again. That is Kimi's
> self-denial-of-service objection **firing**, in the manual path, exactly as
> it predicted. It is survivable only because the worker is hand-run today.
> ITEM 37 must be re-argued against this, not against the paragraph above.

---

## THE COMPASS, AS MEASURED 2026-08-29

```
K1  0.7425   OK            124.0 / 167.0 weight, 16 of 24 axes name a source
K2  20       SOURCE_STALE  ledger 206h old; 20 promotions, 0 withdrawals
K3  2        OK            2 of 3 conclusions from >= 2 sources
K4  —        OK            NO EPOCH reaches the 0.76 coverage floor; best 0.675
```
K2 becomes `NOT_WIRED` once ITEM 14 lands.

**The single most important number found today:** `tools/resolve_ideas.py`
reports a hit rate of **0.0%** over the 10 verdicts that survive leave-one-out —
all 10 are BROKE, there is not one robust HELD. **82.8% of decided verdicts flip
when the last point of each series is dropped.** The unrestricted 44.8% must
never be quoted.

Second: `tools/verify_claims.py` — **0.0% of claims in `memory/*.json` can be
contradicted by anything.** 6 name a vanished key, 1 an address that changed
meaning.

---

## KIMI'S RULINGS TODAY (all binding, all followed)

1. **PRESERVE-AND-MARK** (ITEM 12c) — overruled my DELETE branch. Coverage data
   distinguishing "ran and found nothing" from "did not run" is what the system
   was destroying.
2. **REQUIRE ONE DISPLAY**, then narrowed to **SERVER ONLY** (ITEM 33) — the
   front-end is a distinct consumer; retreat from "display" to "transmit",
   ITEM 36 opened for the renderer.
3. **Option 4** on sequencing — let a shared suite finish; separate only buys
   information when a failure exists.
4. **34-A first**, then **reversed to 34-B first** on my evidence, then
   **34-B DROPPED** entirely — the axes route to `trends.insufficient` and the
   dashboard renders them; the defect was our misdescription.
5. **PROCEED** on wiring `scan()` — overrode its own precondition: "the evidence
   has arrived."
6. **ZERO-SLACK CONSTANT** (ITEM 38).
7. **CADENCE-NEUTRAL** — `min_days` as an additional conjunct beside
   `PROMOTE_AFTER`, because a threshold that loosens when the scheduler changes
   is a hidden invariant.
8. **COMMIT-WHAT-IS-GREEN** (ITEM 32) — with its own objection recorded: the
   seven compass orphans are not pre-existing debt.
9. **RECORD-NOW-ENFORCE-NEXT** — the baseline exists; zero-NEW binds from the
   next batch.
10. **RE-QUALIFY** (ITEM 37) — appending a demotion is a new decision, not a
    rewrite. K2 dropping to zero is honest measurement.
11. **K2-IS-NOT-YET-A-NEEDLE**.
12. **HARDCODE-WITH-EXPIRY** + the detail-preserving `NOT_WIRED` shape.

---

## FACTS A FRESH SESSION WILL OTHERWISE RE-DERIVE

- **`min_days` would be a NEW constraint, not a restored one.** All 20 promotions
  happened within **100–195 seconds** of each source's first observation, inside
  one 261-second burst on 2026-08-20. `PROMOTE_AFTER = 5`'s comment claims "five
  cycles is roughly five nights" — the code has **never** enforced that.
- **Wiring the DMZ worker needs no `config/scheduler.json` edit.**
  `step_budget.ceiling_for` falls back to `_default: 900`; the worker's worst
  case is 11 sources × 30 s = 330 s. `scheduler.json` is guarded — if a change
  ever does need it, that requires Emil's verbatim approval in the commit body.
- **Five declaration sites, not three:** runner `beat()`, `cycle_map.STEPS`,
  `cycle_phases.steps`, `cycle_phases.index_range`, and `_run()` coverage.
  ITEM 12(c) and ITEM 34 each found one the previous count had missed.
- **Baseline of record:** 52–53 failures. 27 are `SUSPENDED_FLAG`
  (`memory/extra_calls_suspended.flag`, self-clearing after one non-breaching
  cycle). 1 is `SCHEDULED_WRITER` (`test_brain_scan`, a coin flip —
  `CORTEX_Approvals` writes `memory/` every minute).
- **Run the suite via `tools/suite_gate.py`, never bare pytest.** A bare run
  gives no verdict on whether a cycle touched the window.
- **Do not stage** `memory/`, `snapshots/`, `data/`, `news/` runtime churn.
  Source under `memory/` (`trend_tracker.py`, `continuous_learner.py`) is fine.
