# ORGANISM — таб 8 в кокпита. Спецификация, 5 септ 2026, 19:55

Емил: „да се вижда как системата реагира с мисълта, духа и тялото си". Правило: всеки
визуален канал е вързан към ИМЕНУВАН измерен сигнал и връзката е написана до него. Канал
без сигнал стои сив с надпис „няма данни от X" — никога замръзнала последна стойност.
Нищо не е „разбиране"; всяка форма е число с име. Шейдър от CPU е скрийнсейвър.

Файлове: `cockpit/organism.html` (p5.js, един canvas, звук по избор — Web Audio, изключен
по подразбиране) + read-only endpoint-и в `cockpit/server.py`: `/api/organism/state`,
`/api/organism/floor`. Нищо в цикъла не се пипа.

| # | Канал | Форма | Източник | Честота |
|---|---|---|---|---|
| 1 | Дишане | ритъм + амплитуда на фигурата | 11 телесни сензора, `/api/somatic` | 5 s, живо |
| 2 | Пулс | импулс по контура при begin, затихване при end; тъмен участък = begin без end | `memory/blackbox.jsonl` | всеки запис (нощем) |
| 3 | Трепване | остро свиване + името на портата и причината | `night_events.jsonl` (refusal) | в мига на отказа |
| 4 | Изненада | разширяване ∝ |грешка| / ширина на интервала | разрешени хипотези (20.05) | при разрешение |
| 5 | Внимание | светване по 36 слоя за подаден вход; attention към токени; адаптер vs база | `tools/brain_pass.py` → JSON | по заявка; свободна карта |
| 6 | Тъкан | loss по стъпка при тренировка; после кои LoRA слоеве са мръднали (||B·A||) | `K1B_*.out.log`, адаптер | 50 s при тренировка |
| 7 | **Глас** | последният запис от THE FLOOR: текст с адаптер; отдолу избледнял текстът на базата, разликите маркирани; две скали P(silent) адаптер/база; ентропия по токен като топлинна лента върху текста; извадките T=1 като бледи сменящи се алтернативи; възраст на записа | `memory/free_expression.jsonl` (`choice_*`, `samples_*`, `divergence`) | в мига на отваряне; иначе последният запис с възраст |

От 12 септ: вход по подразбиране = пясъчникът (наблюдение → действие → последствие →
грешка, която зависи от системата). Панелът се строи веднъж; източникът се сменя.

Ред: канали 1–4 и 7 са само браузър + файлове — след числото на A (готово 18:33).
5 и 6 чакат свободна карта. Ниво на приемане: тест, който пада, ако канал рисува без
именуван източник (endpoint връща `source` за всяка стойност; HTML не съдържа константи).
