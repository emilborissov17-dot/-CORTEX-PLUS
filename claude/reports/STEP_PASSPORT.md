# STEP PASSPORT

Generated 2026-09-12T20:28:47+00:00 by `tools/step_audit.py`. 74 steps.

One mechanical row per step. **No judgement lives here.** What the step is for, which of the 14 points it serves, and the verdict are written by hand in `docs/STEP_LEDGER.md` with a date; this file reads that back only to mark what is still unreviewed.

`—` means NOT KNOWN, and the reason is listed at the bottom. It is never a zero: zero reads as "none of them", a dash reads as "the question could not be answered", and the difference is the point.

**Code or state.** Phase, modules, ceiling, declared outputs, child processes, internal limits and model calls come from the code and the config, so two runs over an unchanged repo agree. History, recent times, kills and *touched* read the record — the baseline, the cycle logs, the supervisor log — and move every night. Every row carries `state_derived` naming them, so a property of this week is not read as a property of the code.

**The kill column is the ledger, and only the ledger.** `kills` counts `CYCLE_KILLED` in `memory/existence_ledger.jsonl`, read off `reason.wedged_step` — the hash-chained record that only a real cycle writes. `logs/supervisor.log` appears in the JSON under `corroboration` and nowhere in this table, because it is poisoned: 720+ of its `KILL POLICY:` lines are fixtures, including 432 for `step='x'`, which is not a step. A fixture repeats verbatim, so `distinct_observations` there says how many real observations hide behind N identical lines.

## Summary

- no internal time cap: **62** of 74
- touch files they do not declare: **24**
- declare files never seen touched: **20**
- spawn a child process: **4**
- never go through `_run()`, so no row is ever written for them: **30**
- no history in the baseline: **31**
    - 30: no _run() in the block — nobody ever writes a row for it
    - 1: the _run() label is absent from the baseline file

## The memory the cycle had (column 14)

A property of the NIGHT, not of any step, which is exactly why it is here: without it, "died in composers" reads as composers' fault. The cycle of 12 Sep 20:04 started with 2063 MB free against a measured appetite of ~1.9 GB typical and 2.7 GB at worst — the night was lost before composers had a turn.

Appetite over the finished cycles in this window: n=9, median 1868 MB, max 2693 MB. An exit row is written on the way out, so a killed cycle contributes none — `—` in the exit column is a missing measurement, not zero memory spent, and is itself the signature of the death.

| cycle | avail at start | avail at exit | consumed | outcome |
|---|--:|--:|--:|---|
| (unmatched) 2026-09-12T09:24:2 | 1761.6 | 1760.8 | 1 | no ledger verdict |
| (unmatched) 2026-09-12T10:09:2 | 3211.3 | 3219.5 | -8 | no ledger verdict |
| (unmatched) 2026-09-12T15:31:1 | 2958.7 | 959.8 | 1999 | no ledger verdict |
| (unmatched) 2026-09-12T15:44:5 | 2818.4 | — | — | no exit row — killed, or still running |
| 2026-09-12T20:04:02.218132+03: | 2062.6 | — | — | CYCLE_DIED |
| 2026-09-12T20:39:02.246817+03: | 2471.3 | 1918.8 | 553 | CYCLE_FINISHED |
| (unmatched) 2026-09-12T19:28:3 | 2724.4 | 2725.5 | -1 | no ledger verdict |
| (unmatched) 2026-09-12T20:07:1 | 3138.7 | 3148.8 | -10 | no ledger verdict |
| (unmatched) 2026-09-12T20:22:1 | 3233.4 | 3228.4 | 5 | no ledger verdict |
| (unmatched) 2026-09-12T20:25:3 | 3505.2 | 3532.0 | -27 | no ledger verdict |


## Steps

| # | step | phase | _run | modules | blk | ceil | n | med | p95 | recent | kills | decl-only | touch-only | both | child | req timeout | count cap | int time cap | retries | model | night | flags | reviewed |
|---|---|---|---|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|---|--:|---|---|---|---|---|---|---|
| -1 | boot | A_ORIENT | **no** | core/model_window.py, core/step_budget.py, core/step_contract.py, core/survival_gate.py | 110 | — | — | — | — | — | 0 | — | — | — | no | 3 | — | yes | Attempt, attempts | no | 2889MB median at start, 1/10 finished | STALE | UNREVIEWED |
| 0 | body_scan | A_ORIENT | **no** | core/groq_backend.py, core/homeostasis.py | 71 | — | — | — | — | — | 0 | — | — | — | yes (1) | 8 | _COOLDOWN_SECS_MAX=180 | yes | — | yes: call_groq, call_groq_meta | 2889MB median at start, 1/10 finished | STALE | UNREVIEWED |
| 0.05 | canon_load | A_ORIENT | **no** | core/canon.py | 16 | — | — | — | — | — | 0 | — | — | — | no | — | MIN_LESSON_CHARS=20, MIN_LESSON_WORDS=3 | yes | — | no | 2889MB median at start, 1/10 finished | MISSING, STALE | 2026-09-12 |
| 0.1 | telegram_approvals | A_ORIENT | **no** | core/brain.py | 14 | — | — | — | — | — | 0 | — | — | — | no | 3 | REPEAT_LIMIT=3, MIN_POOL=8 | yes | — | yes: think | 2889MB median at start, 1/10 finished | STALE, TEST_ONLY | UNREVIEWED |
| 0.2 | brain_briefing | A_ORIENT | **no** | core/brain.py | 18 | — | — | — | — | — | 0 | — | — | — | no | 3 | REPEAT_LIMIT=3, MIN_POOL=8 | yes | — | yes: think | 2889MB median at start, 1/10 finished | STALE, TEST_ONLY | UNREVIEWED |
| 0.25 | notify_patches_and_initiatives | A_ORIENT | **no** | — inline | 4 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 0.5 | dependency_check | A_ORIENT | **no** | core/brain.py | 58 | — | — | — | — | — | 0 | — | — | — | no | 3 | REPEAT_LIMIT=3, MIN_POOL=8 | yes | — | yes: think | 2889MB median at start, 1/10 finished | STALE, TEST_ONLY | UNREVIEWED |
| 0.7 | needs_reanalysis_scan | A_ORIENT | **no** | — inline | 12 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 1 | web_intelligence | B_SENSE | **no** | — inline | 15 | — | — | — | — | 1183.5 | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 2.5 | global_indicators | B_SENSE | **no** | core/global_indicators.py, core/provenance.py, core/source_trust.py | 97 | — | — | — | — | — | 0 | — | — | — | no | 13 | — | no | attempt, attempts | no | 2889MB median at start, 1/10 finished | MISSING, ORPHAN, STALE | UNREVIEWED |
| 2.52 | daily_tier | B_SENSE | yes: daily_tier | core/daily_tier.py | 11 | — | 2 | 1.1 | 1.2 | — | 0 | 1 | 3 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | 2026-09-12 |
| 2.54 | sensorium_ingest | B_SENSE | **no** | — inline | 25 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 2.55 | browser_scout | B_SENSE | **no** | — inline | 14 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 2.6 | composers | B_SENSE | **no** | — inline | 31 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 2.7 | grounding_ledger | B_SENSE | **no** | core/source_trust.py | 21 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | MISSING, ORPHAN, STALE | UNREVIEWED |
| 2.75 | llm_self_review_axes | B_SENSE | **no** | — inline | 5 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 3 | trend_tracker | B_SENSE | **no** | — inline | 4 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 3.5 | cortexstrategist | B_SENSE | yes: cortex_strategist_agent | — inline | 8 | — | 20 | 34.1 | 108.2 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 4 | internet_intelligence | B_SENSE | yes: internet_agent | — inline | 5 | — | 20 | 1394.7 | 1659.6 | — | 6 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 5 | civilization_snapshots | C_SNAPSHOT | yes: civilization_snapshots_agent | — inline | 5 | — | 20 | 19.2 | 58.3 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 6 | planet_snapshots | C_SNAPSHOT | yes: planet_snapshots_agent | — inline | 5 | — | 20 | 41.3 | 83.0 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 7 | human_snapshots | C_SNAPSHOT | yes: human_snapshots_agent | — inline | 5 | — | 20 | 14.8 | 40.1 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 8 | cosmos_snapshots | C_SNAPSHOT | yes: cosmos_snapshots_agent | — inline | 5 | — | 20 | 53.0 | 95.2 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 9 | planetary_potential | C_SNAPSHOT | yes: planetary_potential_agent | — inline | 5 | — | 20 | 22.6 | 73.4 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 10 | energy_review | C_SNAPSHOT | yes: energy_review_agent | — inline | 5 | — | 20 | 19.0 | 40.9 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 11 | self_awareness | C_SNAPSHOT | yes: self_awareness_agent | — inline | 7 | — | 20 | 2.0 | 2.4 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 12 | update_master | D_SCORE | **no** | — inline | 10 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 12.3 | system_hypergraph | D_SCORE | **no** | — inline | 9 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 12.4 | scoring_engine | D_SCORE | **no** | — inline | 50 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 12.42 | alarm_bands | D_SCORE | yes: alarm_bands | core/alarm_bands.py | 6 | — | 20 | 0.6 | 1.0 | — | 0 | 6 | 24 | 2 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | STALE | UNREVIEWED |
| 12.45 | facade_self_check | D_SCORE | **no** | core/scorer_self_check.py | 21 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 12.5 | auto_levels | D_SCORE | **no** | memory/auto_level.py | 18 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 12.55 | level_reconcile | D_SCORE | yes: level_reconcile | core/level_reconciler.py | 14 | — | 20 | 0.5 | 0.6 | — | 0 | 2 | 21 | 2 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 12.56 | axis_history | D_SCORE | yes: axis_history | — inline | 3 | — | 6 | 0.6 | 0.8 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 12.6 | goal_score_calculator | D_SCORE | yes: goal_score_calculator | core/reconsider.py | 45 | — | 20 | 0.5 | 0.9 | — | 0 | 3 | 22 | 0 | no | — | MAX_RETURNS_PER_CYCLE=1, EMPTY_STREAK_LIMIT=3 | no | — | yes: brain.remember, brain.think | 2889MB median at start, 1/10 finished | STALE | UNREVIEWED |
| 12.65 | deduction | D_SCORE | **no** | core/deduction.py | 13 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 12.66 | constancy_and_constellation | D_SCORE | **no** | core/constancy.py | 20 | — | — | — | — | — | 1 | — | — | — | no | — | MIN_POINTS=4 | no | — | yes: brain.numeric_judge, brain.think | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 12.68 | axis_feed | D_SCORE | **no** | — inline | 7 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 12.7 | cognitive_orchestrator | D_SCORE | yes: cortex_orchestrator, orchestrator_grounded | core/cortex_orchestrator.py, core/orchestrator_grounded.py | 29 | — | 40 | 14.6 | 74.3 | — | 0 | 4 | 40 | 1 | no | — | — | no | — | yes: call_groq | 2889MB median at start, 1/10 finished | MISSING, STALE | UNREVIEWED |
| 12.75 | brain_reconsider | D_SCORE | **no** | core/reconsider.py | 16 | — | — | — | — | — | 0 | — | — | — | no | — | MAX_RETURNS_PER_CYCLE=1, EMPTY_STREAK_LIMIT=3 | no | — | yes: brain.remember, brain.think | 2889MB median at start, 1/10 finished | STALE | UNREVIEWED |
| 13 | body_scan | E_PROPOSE | yes: body_scanner | — inline | 5 | — | 20 | 3.6 | 5.2 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 14 | growth_planner | E_PROPOSE | yes: growth_planner | — inline | 6 | — | 20 | 19.8 | 55.4 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 15.6 | hyperclaw | E_PROPOSE | yes: hyperclaw_orchestrator | — inline | 5 | — | 20 | 34.3 | 114.8 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 15.7 | hyperclaw_plan | E_PROPOSE | yes: hyperclaw_plan | core/notary.py | 17 | — | 4 | 0.6 | 0.9 | — | 0 | 1 | 7 | 0 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | MISSING | UNREVIEWED |
| 15.8 | github_publish | E_PROPOSE | yes: github_publisher | — inline | 24 | — | 13 | 50.7 | 54.3 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 16 | action_recommendations | E_PROPOSE | yes: cortex_reasoner | core/cortex_reasoner.py, memory/context_injector.py, memory/semantic_memory.py | 23 | — | 20 | 24.4 | 102.4 | — | 0 | 0 | 36 | 1 | no | — | — | no | — | yes: call_groq | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 17 | self_observer | E_PROPOSE | yes: self_observer | — inline | 5 | — | 20 | 257.3 | 346.5 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 18 | self_modifier | F_SELF | yes: self_modifier | — inline | 6 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 19 | execute_patches | F_SELF | yes: execute_patches | core/metta_check.py | 14 | — | 17 | 0.4 | 0.9 | — | 0 | 3 | 52 | 0 | yes (2) | 1 | — | yes | — | no | 2889MB median at start, 1/10 finished | MISSING | UNREVIEWED |
| 20 | feedback_loop | G_LEARN | yes: feedback_loop | — inline | 17 | — | 20 | 1.3 | 2.0 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 20.05 | resolve_hypotheses | G_LEARN | yes: resolve_hypotheses | core/hypothesis_resolution.py | 17 | — | 9 | 0.6 | 1.0 | — | 0 | 0 | 12 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 20.07 | belief_revision | G_LEARN | yes: belief_revision | core/belief_revision.py | 18 | — | 9 | 0.6 | 1.0 | — | 0 | 1 | 10 | 2 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | TEST_ONLY | UNREVIEWED |
| 20.06 | hypothesis_intake | G_LEARN | yes: hypothesis_intake | core/hypothesis_intake.py, core/measurement_honesty.py | 18 | — | 9 | 0.7 | 1.0 | — | 0 | 7 | 11 | 1 | no | — | MIN_HISTORY=3 | no | — | no | 2889MB median at start, 1/10 finished | STALE | 2026-09-12 |
| 20.1 | measurement_honesty | G_LEARN | yes: measurement_honesty | core/measurement_honesty.py | 26 | — | 16 | 0.6 | 0.6 | — | 0 | 4 | 17 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | STALE | UNREVIEWED |
| 20.2 | resolve_ideas | G_LEARN | yes: resolve_ideas | tools/resolve_ideas.py | 27 | — | 16 | 0.7 | 1.0 | — | 0 | 4 | 17 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | ORPHAN, STALE | UNREVIEWED |
| 21 | session_update | G_LEARN | yes: session_updater | core/session_updater.py | 7 | — | 20 | 2.4 | 2.9 | — | 0 | 0 | 38 | 0 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 22 | daily_analysis | G_LEARN | yes: daily_analysis | — inline | 5 | — | 20 | 684.1 | 1083.7 | — | 6 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 22.5 | data_scout | G_LEARN | yes: data_scout | core/data_scout.py | 14 | — | 20 | 133.8 | 346.8 | — | 0 | 2 | 48 | 1 | no | 2 | — | no | — | no | 2889MB median at start, 1/10 finished | STALE | UNREVIEWED |
| 23 | continuous_learning | G_LEARN | **no** | memory/continuous_learner.py | 12 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | TEST_ONLY | UNREVIEWED |
| 24 | merklememory_commit | G_LEARN | **no** | memory/existence_ledger.py | 100 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | 2026-09-12 |
| 24.1 | merkle_verify | G_LEARN | yes: merkle_verify | — inline | 26 | — | 10 | 0.6 | 0.9 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | 2026-09-12 |
| 25 | training_data_accumulation | G_LEARN | **no** | core/training_log.py | 51 | — | — | — | — | — | 0 | — | — | — | no | — | — | yes | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 25.35 | metta_column | G_LEARN | yes: metta_column | core/metta_parallel.py | 6 | — | 20 | 0.8 | 1.0 | — | 0 | 3 | 21 | 1 | yes (2) | 1 | — | yes | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 25.36 | brain_relay | G_LEARN | yes: brain_relay | core/brain_relay.py | 15 | — | 20 | 0.6 | 22.6 | — | 0 | 1 | 25 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | ORPHAN | UNREVIEWED |
| 25.38 | proposal_sla | G_LEARN | yes: proposal_sla | core/proposal_sla.py | 6 | — | 20 | 0.9 | 1.2 | — | 0 | 3 | 23 | 2 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | ORPHAN, STALE | UNREVIEWED |
| 25.37 | needs_auth | G_LEARN | yes: needs_auth | core/needs_auth.py | 11 | — | 20 | 0.5 | 0.7 | — | 0 | 1 | 21 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | ORPHAN, STALE | UNREVIEWED |
| 25.43 | learn_world | G_LEARN | yes: learn_world | core/learn_world.py | 12 | — | 1 | 1.7 | — | — | 0 | 0 | 4 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 25.44 | self_experiment | G_LEARN | yes: self_experiment | core/self_experiment.py | 10 | — | 20 | 0.5 | 0.6 | — | 0 | 5 | 22 | 1 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | MISSING | UNREVIEWED |
| 25.45 | self_mirror | G_LEARN | yes: self_mirror | core/self_mirror.py | 14 | — | 20 | 0.6 | 0.7 | — | 0 | 4 | 22 | 2 | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 25.46 | read_the_mirror | G_LEARN | yes: read_the_mirror | core/interoception.py | 11 | — | 20 | 172.4 | 225.7 | — | 0 | 4 | 44 | 1 | no | — | MIRROR_QUOTA=2 | yes | — | yes: core.brain | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 25.5 | brain_debrief | G_LEARN | **no** | core/brain.py | 17 | — | — | — | — | — | 0 | — | — | — | no | 3 | REPEAT_LIMIT=3, MIN_POOL=8 | yes | — | yes: think | 2889MB median at start, 1/10 finished | STALE, TEST_ONLY | 2026-09-12 |
| 25.6 | cycle_report | G_LEARN | **no** | core/cycle_report.py | 38 | — | — | — | — | — | 0 | — | — | — | no | — | — | no | — | yes: brain.think | 2889MB median at start, 1/10 finished | - | UNREVIEWED |
| 25.7 | cortex_scan | G_LEARN | yes: cortex_scan | — inline | 32 | — | 16 | 0.8 | 1.3 | — | 0 | — | — | — | no | — | — | no | — | no | 2889MB median at start, 1/10 finished | NO_MODULE | UNREVIEWED |
| 25.8 | compass | G_LEARN | yes: compass | core/blackbox.py, core/cycle_vector.py, core/language_gate.py, core/model_window.py, core/phase_resume.py, core/phase_tracker.py, core/survival_mode.py, memory/autonomic_pulse.py, memory/heartbeat.py, tools/compass.py | 348 | — | 16 | 1.4 | 1.6 | — | 0 | 13 | 16 | 2 | yes (1) | 4 | MIN_CHARS_FOR_DETECTOR=60, MIN_SAMPLE=20 | yes | — | yes: core.backend_policy, core.brain | 2889MB median at start, 1/10 finished | MISSING, ORPHAN, STALE | UNREVIEWED |

## Why a cell is empty

- **-1 boot** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **0 body_scan** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **0.05 canon_load** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **0.1 telegram_approvals** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **0.2 brain_briefing** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **0.25 notify_patches_and_initiatives** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **0.5 dependency_check** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **0.7 needs_reanalysis_scan** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **1 web_intelligence** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **2.5 global_indicators** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **2.52 daily_tier** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: 2 of 3 WARMUP_CYCLES — the contract's verdict stays UNKNOWN until the third run; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **2.54 sensorium_ingest** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **2.55 browser_scout** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **2.6 composers** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **2.7 grounding_ledger** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **2.75 llm_self_review_axes** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **3 trend_tracker** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **3.5 cortexstrategist** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **4 internet_intelligence** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **5 civilization_snapshots** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **6 planet_snapshots** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **7 human_snapshots** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **8 cosmos_snapshots** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **9 planetary_potential** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **10 energy_review** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **11 self_awareness** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **12 update_master** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.3 system_hypergraph** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.4 scoring_engine** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.42 alarm_bands** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **12.45 facade_self_check** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.5 auto_levels** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.55 level_reconcile** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **12.56 axis_history** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **12.6 goal_score_calculator** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived
- **12.65 deduction** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.66 constancy_and_constellation** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.68 axis_feed** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **12.7 cognitive_orchestrator** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **12.75 brain_reconsider** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **13 body_scan** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **14 growth_planner** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **15.6 hyperclaw** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **15.7 hyperclaw_plan** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **15.8 github_publish** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **16 action_recommendations** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **17 self_observer** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **18 self_modifier** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: the block calls _run(self_modifier) but no such key exists in step_contract_baseline.json. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **19 execute_patches** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **20 feedback_loop** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **20.05 resolve_hypotheses** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **20.07 belief_revision** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **20.06 hypothesis_intake** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived
- **20.1 measurement_honesty** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **20.2 resolve_ideas** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **21 session_update** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **22 daily_analysis** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **22.5 data_scout** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **23 continuous_learning** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **24 merklememory_commit** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **24.1 merkle_verify** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25 training_data_accumulation** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **25.35 metta_column** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.36 brain_relay** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.38 proposal_sla** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.37 needs_auth** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.43 learn_world** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: 1 of 3 WARMUP_CYCLES — the contract's verdict stays UNKNOWN until the third run; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.44 self_experiment** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.45 self_mirror** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.46 read_the_mirror** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived
- **25.5 brain_debrief** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **25.6 cycle_report** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); history: no _run() call inside the block, so no StepContract is ever opened for it — nobody writes rows. Writer: core/step_contract.py, opened by fast_cycle_runner._run(); beat() writes a heartbeat, not a contract; recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: no history, so nothing is known to have been touched — declared_only here is NOT evidence of a dead output; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan; _run: the block calls beat() but never _run(), so StepContract is never opened and no row is ever written for it
- **25.7 cortex_scan** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived; declared vs touched: the block names no module, so there are no constants to read a declaration from; count cap: no module-level int constant named MAX_*/*_LIMIT/*_CAP/*_QUOTA/*_PER_*; a cap inside a function body is invisible to a constant scan
- **25.8 compass** — ceiling: core.step_budget.effective_ceiling unavailable (ModuleNotFoundError: No module named 'core'); recent times: the block prints no '(Ns)' of its own, and cycle-log lines carry no timestamps, so no duration can be derived
